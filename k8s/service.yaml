apiVersion: v1
kind: Service
metadata:
  name: joshualowrance-com
  labels:
    app: joshualowrance-com
spec:
  selector:
    app: joshualowrance-com
  ports:
    - port: 80
      targetPort: 3000
      protocol: TCP
  type: ClusterIP
